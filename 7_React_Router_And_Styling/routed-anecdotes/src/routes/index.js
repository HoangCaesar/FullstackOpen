import { useRoutes } from 'react-router-dom';

// project import
import MainRoute from './MainRoute';

// ==============================|| ROUTES APP  ||============================== //

const AppRoutes = () => useRoutes([MainRoute]);

export default AppRoutes;
